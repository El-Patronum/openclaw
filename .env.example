# OpenClaw .env example
#
# Quick start:
# 1) Copy this file to `.env` (for local runs from this repo), OR to `~/.openclaw/.env` (for launchd/systemd daemons).
# 2) Fill only the values you use.
# 3) Keep real secrets out of git.
#
# Env-source precedence for environment variables (highest -> lowest):
# process env, ./.env, ~/.openclaw/.env, then openclaw.json `env` block.
# Existing non-empty process env vars are not overridden by dotenv/config env loading.
# Note: direct config keys (for example `gateway.auth.token` or channel tokens in openclaw.json)
# are resolved separately from env loading and often take precedence over env fallbacks.

# -----------------------------------------------------------------------------
# Gateway auth + paths
# -----------------------------------------------------------------------------
# Recommended if the gateway binds beyond loopback.
OPENCLAW_GATEWAY_TOKEN=change-me-to-a-long-random-token
# Example generator: openssl rand -hex 32

# Optional alternative auth mode (use token OR password).
# OPENCLAW_GATEWAY_PASSWORD=change-me-to-a-strong-password

# Optional path overrides (defaults shown for reference).
# OPENCLAW_STATE_DIR=~/.openclaw
# OPENCLAW_CONFIG_PATH=~/.openclaw/openclaw.json
# OPENCLAW_HOME=~

# Optional: import missing keys from your login shell profile.
# OPENCLAW_LOAD_SHELL_ENV=1
# OPENCLAW_SHELL_ENV_TIMEOUT_MS=15000

# -----------------------------------------------------------------------------
# Model provider API keys (set at least one)
# -----------------------------------------------------------------------------
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# GEMINI_API_KEY=...
# OPENROUTER_API_KEY=sk-or-...

# Optional additional providers
# ZAI_API_KEY=...
# AI_GATEWAY_API_KEY=...
# MINIMAX_API_KEY=...
# SYNTHETIC_API_KEY=...

# -----------------------------------------------------------------------------
# Channels (only set what you enable)
# -----------------------------------------------------------------------------
# TELEGRAM_BOT_TOKEN=123456:ABCDEF...
# DISCORD_BOT_TOKEN=...
# SLACK_BOT_TOKEN=xoxb-...
# SLACK_APP_TOKEN=xapp-...

# Optional channel env fallbacks
# MATTERMOST_BOT_TOKEN=...
# MATTERMOST_URL=https://chat.example.com
# ZALO_BOT_TOKEN=...
# OPENCLAW_TWITCH_ACCESS_TOKEN=oauth:...

# -----------------------------------------------------------------------------
# Tools + voice/media (optional)
# -----------------------------------------------------------------------------
# BRAVE_API_KEY=...
# PERPLEXITY_API_KEY=pplx-...
# FIRECRAWL_API_KEY=...

# ELEVENLABS_API_KEY=...
# XI_API_KEY=...  # alias for ElevenLabs
# DEEPGRAM_API_KEY=...

# -----------------------------------------------------------------------------
# Docker build/runtime knobs (VPS + Tailscale profile)
# -----------------------------------------------------------------------------
# Image/tag used by docker-compose.yml
# OPENCLAW_IMAGE=openclaw:local
#
# Host-mounted persistent dirs (required for durable config/state/workspace)
# OPENCLAW_CONFIG_DIR=~/.openclaw
# OPENCLAW_WORKSPACE_DIR=~/.openclaw/workspace
#
# Keep host publish loopback-only and expose remotely via SSH tunnel or Tailscale.
# Set OPENCLAW_GATEWAY_HOST=0.0.0.0 for Tailscale/LAN access.
# OPENCLAW_GATEWAY_HOST=127.0.0.1
# OPENCLAW_GATEWAY_PORT=18789
# OPENCLAW_GATEWAY_BIND=lan
#
# Extra build-time apt packages (persistent across container recreates)
# OPENCLAW_DOCKER_APT_PACKAGES="jq ripgrep"
# OPENCLAW_PREBUILD_NODE_LLAMA=1
#
# Optional build-time npm global CLIs (persistent across container recreates)
# OPENCLAW_INSTALL_CLAUDE=1
# OPENCLAW_CLAUDE_NPM_PACKAGE=@anthropic-ai/claude-code@2.1.42
# OPENCLAW_INSTALL_GEMINI=1
# OPENCLAW_GEMINI_NPM_PACKAGE=@google/gemini-cli@0.28.2
# OPENCLAW_DOCKER_NPM_GLOBAL_PACKAGES="mcporter@0.7.3"
#
# Optional build-time Go CLIs (space-separated `go install` package specs)
# OPENCLAW_DOCKER_GO_PACKAGES="github.com/Hyaxia/blogwatcher/cmd/blogwatcher@latest"
#
# Optional local speech-to-text in container (OpenAI Whisper CLI via pip + ffmpeg)
# OPENCLAW_INSTALL_WHISPER=1
# OPENCLAW_INSTALL_UV=1
# OPENCLAW_INSTALL_GOG=1
# OPENCLAW_GOG_DOWNLOAD_URL=
#
# Optional Linuxbrew formula installs (persistent across recreates)
# OPENCLAW_INSTALL_BREW=1
# OPENCLAW_BREW_TAPS="steipete/tap yakitrak/yakitrak"
# OPENCLAW_BREW_FORMULAS="steipete/tap/summarize yakitrak/yakitrak/obsidian-cli"
#
# Skip interactive onboarding during scripted rebuilds:
# OPENCLAW_SKIP_ONBOARD=1
#
# Rebuild gates (comma-separated lists validated after startup)
# OPENCLAW_REQUIRED_BINS=claude,gog,gemini,mcporter,uv,whisper,jq,rg,gh,summarize,obsidian-cli
# OPENCLAW_REQUIRED_SKILLS=coding-agent,gog,gemini,github,mcporter,obsidian,openai-whisper,session-logs,summarize
